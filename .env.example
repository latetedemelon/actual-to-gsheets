# Actual Budget Server Configuration
ACTUAL_SERVER_URL=https://your-actual-server.com
ACTUAL_PASSWORD=your-actual-password
ACTUAL_FILE=My-Budget-File
ACTUAL_ENCRYPTION_PASSWORD=your-encryption-password-if-needed

# Google Sheets Configuration
GOOGLE_SHEET_ID=your-google-sheet-id
GOOGLE_CREDENTIALS_FILE=service-account-credentials.json

# Transaction Export Configuration (Optional)
# Set to true to export transactions to a separate sheet
EXPORT_TRANSACTIONS=false
# Options: current_month, previous_month, both_months
TRANSACTIONS_DATE_RANGE=current_month
